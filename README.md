# Telegram бот для выдачи VPN конфигураций из x-ui

Этот бот позволяет получать VPN конфигурации из панели управления x-ui через Telegram.

## Возможности

- 📋 Просмотр списка всех inbounds
- 👥 Просмотр клиентов для каждого inbound
- 📥 Получение конфигураций VPN клиентов
- 🔐 Поддержка ограничения доступа по Telegram ID
- 🎯 Поддержка VLESS, VMESS и Trojan протоколов
- 👨‍💼 Админский функционал (добавление пользователей, управление лимитами)
- 📊 Система лимитов на количество конфигов для каждого пользователя
- ⏰ Автоматические напоминания о истечении конфигов (за 10 и 3 дня)

## Структура проекта

```
.
├── bot.py              # Основной файл бота
├── database.py         # Модуль для работы с базой данных
├── xui_client.py       # Модуль для работы с x-ui API
├── config.py.example   # Пример файла конфигурации
├── requirements.txt    # Зависимости проекта
├── README.md           # Документация
└── .gitignore          # Игнорируемые файлы Git

# Файлы, которые создаются при работе:
├── config.py           # Файл конфигурации (не коммитится в git)
└── bot.db              # База данных SQLite (не коммитится в git)
```

## Установка

1. Установите Python 3.8 или выше

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка

### 1. Получение Telegram Bot Token

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot` и следуйте инструкциям
3. Сохраните полученный токен

### 2. Настройка x-ui

Убедитесь, что ваша x-ui панель доступна по API и у вас есть учетные данные для входа.

### 3. Конфигурация бота

Скопируйте файл `config.py.example` в `config.py`:

```bash
cp config.py.example config.py
```

Затем откройте файл `config.py` и настройте следующие параметры:

```python
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN = "ваш_токен_бота"

# x-ui API настройки
XUI_BASE_URL = "http://ваш-сервер:54321"  # URL вашей x-ui панели
XUI_USERNAME = "ваш_логин"                # Логин для x-ui
XUI_PASSWORD = "ваш_пароль"               # Пароль для x-ui

# Список разрешенных Telegram username пользователей (опционально, оставьте пустым для открытого доступа)
# Указывайте username без символа @
ALLOWED_USERNAMES = [
    # Пример: "username1", "username2"
]

# Администраторы бота (Telegram username без @)
ADMIN_USERNAMES = [
    # Пример: "your_username"
    # Добавьте сюда username администраторов
]

# Настройки напоминаний
REMINDER_CHECK_INTERVAL = 3600  # Интервал проверки (в секундах, по умолчанию 1 час)
REMINDER_DAYS = [10, 3]  # Напоминания за N дней до истечения
```

Или используйте переменные окружения:

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен"
export XUI_BASE_URL="http://ваш-сервер:54321"
export XUI_USERNAME="ваш_логин"
export XUI_PASSWORD="ваш_пароль"
```

## Запуск

```bash
python bot.py
```

## Использование

После запуска бота отправьте команду `/start` в Telegram.

### Доступные команды для пользователей:

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/myinfo` - Показать информацию о вашем аккаунте (лимиты, использовано)
- `/list` - Показать список всех inbounds
- `/clients <inbound_id>` - Показать клиентов для указанного inbound
- `/get <email>` - Получить конфигурацию клиента по email

### Админские команды:

- `/adminhelp` - Справка по админским командам
- `/adduser <username> <limit>` - Добавить пользователя по username и установить лимит
- `/setlimit <username> <limit>` - Изменить лимит конфигов для пользователя
- `/users` - Показать список всех пользователей
- `/sync_reminders` - Синхронизировать напоминания из x-ui

### Примеры:

```
# Пользовательские команды
/list
/clients 1
/get user@example.com
/myinfo

# Админские команды
/adduser @username 5
/setlimit @username 10
/users
```

## Безопасность

- Не храните токены и пароли в публичных репозиториях
- Используйте переменные окружения для секретных данных
- Настройте список разрешенных пользователей для ограничения доступа
- Рекомендуется использовать VPN или прокси для доступа к x-ui API

## Система лимитов и напоминаний

### Лимиты конфигов

Бот поддерживает систему лимитов для каждого пользователя:
- Администратор может установить лимит конфигов для каждого пользователя
- При попытке получить конфиг проверяется наличие доступных лимитов
- Лимит автоматически уменьшается при выдаче конфига

### Напоминания

Бот автоматически отслеживает истечение VPN конфигов:
- Автоматически синхронизирует информацию о конфигах из x-ui
- Отправляет напоминания за 10 дней и за 3 дня до истечения
- Напоминания проверяются каждый час (настраивается в config.py)

**Важно:** После создания конфигов в x-ui используйте команду `/sync_reminders` для синхронизации напоминаний.

### Как добавить пользователя

1. Попросите пользователя написать боту `/start` (чтобы он зарегистрировался в базе)
2. Используйте команду `/users` чтобы увидеть всех пользователей и их username
3. Используйте команду `/setlimit <username> <limit>` для установки лимита

**Важно:** 
- Пользователь должен иметь username в Telegram для работы с командами по username
- Если у пользователя нет username, он может использовать бота, но администратор должен будет установить лимит через базу данных напрямую
- Username можно указывать с символом @ или без него в командах

## Примечания

- Убедитесь, что x-ui панель доступна с сервера, где запущен бот
- Проверьте, что API x-ui включен и работает
- Конфигурации генерируются на основе настроек вашего x-ui
- База данных (bot.db) создается автоматически при первом запуске
- Для работы напоминаний необходимо периодически синхронизировать данные из x-ui

## Устранение неполадок

**Бот не отвечает:**
- Проверьте правильность токена бота
- Убедитесь, что бот запущен

**Ошибка подключения к x-ui:**
- Проверьте правильность URL, логина и пароля
- Убедитесь, что x-ui панель доступна
- Проверьте настройки файрвола

**Не найдены клиенты:**
- Убедитесь, что в x-ui есть созданные клиенты
- Проверьте правильность ID inbound

## Лицензия

Этот проект предоставляется "как есть" без каких-либо гарантий.

