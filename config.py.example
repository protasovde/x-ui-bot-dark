"""
Конфигурационный файл для Telegram бота x-ui
Скопируйте этот файл в config.py и заполните своими данными

ВАЖНО: Не коммитьте config.py в git! Этот файл содержит секретные данные.
Используйте переменные окружения для безопасного хранения токенов и паролей.
"""
import os
from typing import Optional

# Telegram Bot Token (получить у @BotFather)
# Можно установить через переменную окружения: export TELEGRAM_BOT_TOKEN="ваш_токен"
TELEGRAM_BOT_TOKEN: Optional[str] = os.getenv("TELEGRAM_BOT_TOKEN", "")

# x-ui API настройки
# Можно установить через переменные окружения для безопасности
XUI_BASE_URL: str = os.getenv("XUI_BASE_URL", "http://your-server:54321")
XUI_USERNAME: str = os.getenv("XUI_USERNAME", "your_username")
XUI_PASSWORD: str = os.getenv("XUI_PASSWORD", "your_password")

# Список разрешенных Telegram username пользователей (опционально, оставьте пустым для открытого доступа)
# Указывайте username без символа @
ALLOWED_USERNAMES: list[str] = [
    # Пример: "username1", "username2"
]

# ID inbound по умолчанию (захардкожен для всех пользователей)
DEFAULT_INBOUND_ID: int = 7

# Срок действия конфига в днях
CONFIG_EXPIRY_DAYS: int = 31

# Администраторы бота (Telegram username без @)
# Можно установить через переменную окружения ADMIN_USERNAMES (через запятую)
admin_env = os.getenv("ADMIN_USERNAMES", "")
if admin_env:
    ADMIN_USERNAMES: list[str] = [u.strip() for u in admin_env.split(",") if u.strip()]
else:
    ADMIN_USERNAMES: list[str] = [
        "ImmoLateNeltharion"
    ]

# File ID видео инструкции (опционально)
# Чтобы получить file_id, отправьте видео боту и получите его из ответа API
# Можно установить через переменную окружения: export INSTRUCTION_VIDEO_FILE_ID="file_id"
INSTRUCTION_VIDEO_FILE_ID: Optional[str] = os.getenv("INSTRUCTION_VIDEO_FILE_ID", "")

# Интервал проверки напоминаний (в секундах)
REMINDER_CHECK_INTERVAL: int = 3600  # 1 час

# Напоминания за N дней до истечения
REMINDER_DAYS: list[int] = [10, 3]

